{
	"description": "The specified resource does not exist, or the authenticated user does not have permission to view it.",
	"content": {
		"application/json": {
			"schema": {
				"$ref": "../schemas/PdcError.json"
			}
		}
	}
}

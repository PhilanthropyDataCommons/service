{
	"type": "object",
	"properties": {
		"proposalFieldValue": {
			"$ref": "./PermissionGrantConditionLeaf.json"
		}
	},
	"additionalProperties": false,
	"description": "Optional conditions that restrict when this permission grant applies. Each key must be an entity type present in the grant's `scope` array; keys not in `scope` will be rejected."
}

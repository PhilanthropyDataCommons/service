{
	"type": "object",
	"properties": {
		"proposalFieldValue": {
			"$ref": "./PermissionGrantConditionRule.json"
		},
		"initiativeFieldValue": {
			"$ref": "./PermissionGrantConditionRule.json"
		}
	},
	"additionalProperties": false,
	"description": "Optional conditions that restrict when this permission grant applies. Currently, only the `proposalFieldValue` and `initiativeFieldValue` conditions are supported. Each key must be an entity type present in the grant's `scope` array; keys not in `scope` will be rejected."
}

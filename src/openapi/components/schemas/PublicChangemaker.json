{
	"type": "object",
	"description": "The changemaker attributes returned to every caller, including callers that do not hold `view` permission on the changemaker and unauthenticated callers.",
	"properties": {
		"id": {
			"$ref": "./id.json",
			"readOnly": true,
			"example": 3407
		},
		"taxId": {
			"type": "string",
			"example": "11-1111111"
		},
		"name": {
			"type": "string",
			"example": "Example Inc."
		}
	},
	"required": ["id", "taxId", "name"]
}

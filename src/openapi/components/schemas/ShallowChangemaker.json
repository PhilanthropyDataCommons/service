{
	"description": "A changemaker without its children (`fields` and `fiscalSponsors`). Which attributes are present depends on whether the caller holds `view` permission on this particular changemaker.",
	"allOf": [
		{
			"$ref": "./PublicChangemaker.json"
		},
		{
			"$ref": "./ProtectedChangemakerAttributes.json"
		}
	]
}
